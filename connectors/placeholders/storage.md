# 网盘或对象存储 MCP 占位

## 适用系统类型

网盘或对象存储。本文件仅为私有部署规划占位，不包含真实连接器实现。

## 可读数据类型

指定文件夹内的文件和元数据。

## 可写数据类型

经确认的脱敏输出。默认不写回，所有写回必须人工确认。

## 推荐默认权限

只读指定路径。

## 禁止动作

同步全盘、公开分享、删除或移动原始材料。

## 适合哪些插件调用

三个核心插件。

## 部署前需要确认

- 客户授权和事项范围。
- 访问控制、审计日志和撤销机制。
- 是否涉及个人信息、商业秘密或跨境传输。
- 是否允许写回，以及谁有权确认写回。
