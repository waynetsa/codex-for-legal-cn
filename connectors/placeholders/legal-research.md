# 法研数据库 MCP 占位

## 适用系统类型

法研数据库。本文件仅为私有部署规划占位，不包含真实连接器实现。

## 可读数据类型

法规、案例、监管规则、检索结果。

## 可写数据类型

通常不写入。默认不写回，所有写回必须人工确认。

## 推荐默认权限

只读检索并保留来源。

## 禁止动作

把检索结果直接作为法律意见。

## 适合哪些插件调用

三个核心插件。

## 部署前需要确认

- 客户授权和事项范围。
- 访问控制、审计日志和撤销机制。
- 是否涉及个人信息、商业秘密或跨境传输。
- 是否允许写回，以及谁有权确认写回。
